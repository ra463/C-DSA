cout << "Enter the value of p, y and r" << endl;
    // cin >> p >> y >> r;
    // bank b1(p, y, r);
    // b1.showdata();

    // cout << "Enter the value of p, y and R" << endl;
    // cin >> p >> y >> R;
    // bank b2(p, y, R);
    // b2.showdata();